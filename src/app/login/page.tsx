// Временная страница входа — NextAuth redirect fallback
// TODO Sprint 3: заменить redirect на /[ADMIN_SLUG]/login через middleware
// Эта страница не раскрывает ADMIN_SLUG
export default function LoginPage() {
  return null
}
